'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Sample business seeding logic
    // This would create sample businesses for development/testing
  },

  down: async (queryInterface) => {
    // Remove sample businesses
  },
};

